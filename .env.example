# ============================================================================
# AWS Elastic Beanstalk + RDS Environment Configuration
# For local development, copy to .env and fill with local values
# For EB deployment, set these via 'eb setenv' command
# ============================================================================

# ============================================================================
# DATABASE CONFIGURATION (AWS RDS)
# ============================================================================
# For AWS Elastic Beanstalk deployment, use DATABASE_URL format:
DATABASE_URL=mysql://admin:your-password@your-rds-endpoint.ap-southeast-1.rds.amazonaws.com:3306/studentattendance

# OR use individual RDS variables (alternative method):
# RDS_HOSTNAME=your-rds-endpoint.ap-southeast-1.rds.amazonaws.com
# RDS_PORT=3306
# RDS_DB_NAME=studentattendance
# RDS_USERNAME=admin
# RDS_PASSWORD=your-secure-password

# For local development (if not using RDS):
# DB_HOST=127.0.0.1
# DB_USER=root
# DB_PASSWORD=
# DB_NAME=studentattendance
# DB_PORT=3306

# ============================================================================
# FLASK CONFIGURATION
# ============================================================================
FLASK_ENV=production
FLASK_APP=main.py
PORT=5000

# ============================================================================
# DEPLOYMENT ENVIRONMENT & URL CONFIGURATION
# ============================================================================
# For AWS EB deployment:
ENVIRONMENT=production
URL_PROTOCOL=https
DOMAIN_NAME=fyp-flask-env.eba-8ev8txxm.ap-southeast-1.elasticbeanstalk.com

# For local development:
# ENVIRONMENT=development
# URL_PROTOCOL=http
# DOMAIN_NAME=localhost

# ============================================================================
# EMAIL CONFIGURATION (for notifications)
# ============================================================================
# Email Configuration for Gmail SMTP
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-google-app-password-here
EMAIL_FROM=UOW Student Attendance System <your-email@gmail.com>

# ============================================================================
# AWS ELASTIC BEANSTALK DEPLOYMENT INSTRUCTIONS
# ============================================================================
# 1. Set RDS credentials via EB environment variables:
#    eb setenv DATABASE_URL="mysql://admin:password@rds-endpoint:3306/student_attendance"
#    eb setenv DOMAIN_NAME="your-eb-domain.elasticbeanstalk.com"
#
# 2. Deploy changes:
#    eb deploy
#
# 3. View logs:
#    eb logs
#    eb health --refresh
#
# 4. SSH to instance (for debugging):
#    eb ssh

# ============================================================================
# Notes:
# 1. LOCAL: Don't set DATABASE_URL. App checks:
#    - RDS_* env vars → if set, uses AWS RDS
#    - else → defaults to localhost:3306
#
# 2. AWS EB: Set DATABASE_URL via 'eb setenv' for RDS connection
#    - All settings should be in EB environment, not checked in .env
#
# 3. EMAIL: Gmail requires 2FA + App Password (not your regular password)
#    - Format: mysql://user:pass@host:port/database
#
# 3. AWS RDS: Set RDS_HOSTNAME, RDS_USERNAME, RDS_PASSWORD
# ============================================================================
