# ============================================================================
# Railway.app Environment Configuration
# Copy to .env.local and fill values for local development
# ============================================================================

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# Option 1: Railway's auto-generated DATABASE_URL (Railway sets this automatically)
# DATABASE_URL=mysql://username:password@railway-db-host:3306/student_attendance

# Option 2: AWS RDS (if using RDS instead of Railway)
# RDS_HOSTNAME=your-rds-endpoint.rds.amazonaws.com
# RDS_PORT=3306
# RDS_DB_NAME=student_attendance
# RDS_USERNAME=admin
# RDS_PASSWORD=your-rds-password

# Option 3: Local Development (default - leave DATABASE_URL and RDS_* unset)
# Connects to: localhost:3306

# ============================================================================
# FLASK CONFIGURATION
# ============================================================================
FLASK_ENV=production
FLASK_APP=main.py
PORT=5000

# ============================================================================
# EMAIL CONFIGURATION (for notifications)
# ============================================================================
# Email Configuration for Gmail SMTP
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-google-app-password-here
EMAIL_FROM=UOW Student Attendance System <your-email@gmail.com>

# ============================================================================
# Notes:
# 1. LOCAL: Don't set DATABASE_URL. App checks:
#    - RDS_* env vars → if set, uses AWS RDS
#    - else → defaults to localhost:3306
#
# 2. RAILWAY: Set DATABASE_URL in Railway dashboard
#    - Railway auto-provides when MySQL provisioned
#    - Format: mysql://user:pass@host:port/database
#
# 3. AWS RDS: Set RDS_HOSTNAME, RDS_USERNAME, RDS_PASSWORD
# ============================================================================
